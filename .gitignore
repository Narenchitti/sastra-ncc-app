# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# local env files
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# User Data (Since we moved to Supabase, we can ignore local JSONs if we want to keep repo clean, but maybe keep for reference? Let's ignore to strictly follow cloud pattern)
/data/*.json
